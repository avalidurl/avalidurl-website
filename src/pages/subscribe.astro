---
import Layout from '../layouts/Layout.astro';
---

<Layout title="Subscribe - Stay Updated" description="Subscribe to receive insights on finance, art, and technology directly in your inbox.">
  <main class="container">
    <div class="subscribe-page">
      <header class="subscribe-header">
        <h1>Stay in the Loop</h1>
        <p class="subscribe-subtitle">
          Get notified when I publish new insights on the intersection of finance, art, and technology.
        </p>
      </header>
      
      <div class="subscribe-content">
        <div class="subscribe-form-container">
          <h2>Newsletter Subscription</h2>
          <p class="form-description">
            Join a community of creators, developers, and financial professionals exploring 
            the convergence of data, design, and market dynamics.
          </p>
          
          <form class="subscribe-form" action="https://app.convertkit.com/forms/6c28efde16/subscriptions" method="post" data-sv-form="6c28efde16" data-uid="6c28efde16" data-format="inline" data-version="5">
            <div class="form-group">
              <label for="email_address">Email Address</label>
              <input 
                type="email" 
                id="email_address" 
                name="email_address" 
                placeholder="your@email.com" 
                required
              >
            </div>
            
            <div class="form-group">
              <label for="fields[interests]">What interests you most?</label>
              <select id="fields[interests]" name="fields[interests]">
                <option value="all">Everything</option>
                <option value="finance">Financial Analysis & Tools</option>
                <option value="art">Creative Coding & Design</option>
                <option value="intersection">Finance + Art Intersection</option>
                <option value="technology">Technology & Innovation</option>
              </select>
            </div>
            
            <button type="submit" class="subscribe-button">
              Subscribe to Newsletter
            </button>
          </form>
          
          <p class="privacy-note">
            No spam, ever. Unsubscribe at any time. 
            <a href="/privacy">Privacy Policy</a>
          </p>
        </div>
        
        <div class="subscribe-benefits">
          <h3>What You'll Get</h3>
          <ul class="benefits-list">
            <li>
              <strong>Weekly Insights</strong>
              Deep dives into market psychology and algorithmic beauty
            </li>
            <li>
              <strong>Tool Updates</strong>
              First access to new financial tracking and visualization tools
            </li>
            <li>
              <strong>Behind the Scenes</strong>
              The design and development process behind financial art projects
            </li>
            <li>
              <strong>Exclusive Content</strong>
              Subscriber-only analysis and early access to new ideas
            </li>
          </ul>
          
          <div class="alternative-feeds">
            <h4>Prefer RSS?</h4>
            <p>
              You can also follow along via 
              <a href="/feed.xml" class="rss-link">RSS feed</a>
              if you prefer feed readers.
            </p>
          </div>
        </div>
      </div>
      
      <div class="subscribe-footer">
        <p>
          Ready to explore where finance meets art? 
          <a href="/blog">Check out recent posts</a> or 
          <a href="/">view my latest projects</a>.
        </p>
      </div>
    </div>
  </main>
</Layout>

<style>
  .subscribe-page {
    max-width: 800px;
    margin: 0 auto;
  }
  
  .subscribe-header {
    text-align: center;
    margin-bottom: 3rem;
    padding-bottom: 2rem;
    border-bottom: 1px solid var(--border-light);
    position: relative;
  }
  
  .subscribe-header::after {
    content: '';
    position: absolute;
    bottom: -1px;
    left: 50%;
    transform: translateX(-50%);
    width: 80px;
    height: 1px;
    background: var(--accent-primary);
  }
  
  .subscribe-subtitle {
    font-size: 1.125rem;
    color: var(--text-secondary);
    margin-top: 1rem;
    line-height: 1.6;
    font-style: italic;
  }
  
  .subscribe-content {
    display: grid;
    grid-template-columns: 1.3fr 0.7fr;
    gap: 5rem;
    margin-bottom: 3rem;
    align-items: start;
  }
  
  .subscribe-form-container {
    background: var(--bg-card);
    padding: 2.5rem;
    border: 2px solid var(--border-light);
    border-radius: 8px;
  }
  
  .subscribe-form-container h2 {
    color: var(--text-primary);
    margin-bottom: 1rem;
    font-family: 'Georgia', serif;
  }
  
  .form-description {
    color: var(--text-secondary);
    line-height: 1.6;
    margin-bottom: 2rem;
  }
  
  .form-group {
    margin-bottom: 1.5rem;
  }
  
  .form-group label {
    display: block;
    color: var(--text-secondary);
    font-weight: 600;
    margin-bottom: 0.5rem;
    font-size: 0.9rem;
    letter-spacing: 0.3px;
  }
  
  .form-group input,
  .form-group select {
    width: 100%;
    padding: 0.75rem;
    border: 2px solid var(--border-medium);
    border-radius: 6px;
    background: var(--bg-primary);
    color: var(--text-primary);
    font-size: 1rem;
    transition: border-color 0.3s ease;
  }
  
  .form-group input:focus,
  .form-group select:focus {
    outline: none;
    border-color: var(--accent-primary);
    box-shadow: 0 0 0 3px rgba(212, 175, 55, 0.1);
  }
  
  .subscribe-button {
    width: 100%;
    background: var(--accent-primary);
    color: var(--text-on-dark);
    border: none;
    padding: 1rem 2rem;
    border-radius: 6px;
    font-size: 1rem;
    font-weight: 600;
    letter-spacing: 0.5px;
    cursor: pointer;
    transition: all 0.3s ease;
    margin-bottom: 1rem;
  }
  
  .subscribe-button:hover {
    background: var(--accent-secondary);
    box-shadow: 0 4px 12px rgba(212, 175, 55, 0.3);
    transform: translateY(-1px);
  }
  
  .privacy-note {
    font-size: 0.875rem;
    color: var(--text-muted);
    text-align: center;
  }
  
  .privacy-note a {
    color: var(--accent-primary);
    font-weight: 500;
  }
  
  .subscribe-benefits {
    background: var(--blog-parchment);
    padding: 2rem;
    border: 1px solid var(--border-light);
    border-radius: 8px;
    height: fit-content;
  }
  
  .subscribe-benefits h3 {
    color: var(--text-primary);
    margin-bottom: 1.5rem;
    font-family: 'Georgia', serif;
  }
  
  .benefits-list {
    list-style: none;
    padding: 0;
    margin: 0 0 2rem 0;
  }
  
  .benefits-list li {
    margin-bottom: 1.25rem;
    padding-left: 1.5rem;
    position: relative;
    color: var(--text-secondary);
    line-height: 1.5;
  }
  
  .benefits-list li::before {
    content: '✦';
    position: absolute;
    left: 0;
    color: var(--accent-primary);
    font-weight: bold;
  }
  
  .benefits-list strong {
    color: var(--text-primary);
    display: block;
    margin-bottom: 0.25rem;
  }
  
  .alternative-feeds {
    border-top: 1px solid var(--border-light);
    padding-top: 1.5rem;
    margin-top: 2rem;
  }
  
  .alternative-feeds h4 {
    color: var(--text-primary);
    margin-bottom: 0.75rem;
    font-size: 1.05rem;
  }
  
  .alternative-feeds p {
    color: var(--text-secondary);
    font-size: 0.95rem;
    line-height: 1.6;
  }
  
  .rss-link {
    color: var(--accent-primary);
    font-weight: 600;
    padding: 0.25rem 0.5rem;
    background: rgba(212, 175, 55, 0.1);
    border-radius: 4px;
    text-decoration: none;
    border-bottom: 2px solid var(--accent-primary);
    transition: all 0.3s ease;
  }
  
  .rss-link:hover {
    background: var(--accent-primary);
    color: var(--text-on-dark);
    border-bottom-color: var(--accent-primary);
    transform: translateY(-1px);
  }
  
  .subscribe-footer {
    text-align: center;
    padding-top: 2rem;
    border-top: 1px solid var(--border-light);
    color: var(--text-secondary);
  }
  
  .subscribe-footer a {
    color: var(--accent-primary);
    font-weight: 500;
  }
  
  @media (max-width: 768px) {
    .subscribe-content {
      grid-template-columns: 1fr;
      gap: 2rem;
    }
    
    .subscribe-form-container,
    .subscribe-benefits {
      padding: 2rem;
    }
  }
</style>